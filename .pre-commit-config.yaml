repos:
  - repo: local
    hooks:
      - id: make-format
        name: Run format
        entry: make format
        language: system
        files: ^src/.*$

  - repo: https://github.com/crate-ci/typos
    rev: v1
    hooks:
      - id: typos
        files: ^(src|proto|examples)/.*$
        args: ["--force-exclude"]
